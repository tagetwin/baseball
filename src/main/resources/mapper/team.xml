<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baseball.springboot.repository.TeamRepository">

	<select id="findAll" resultType="com.baseball.springboot.model.Team">
		select * from Team order by tId desc
	</select>
	
</mapper>